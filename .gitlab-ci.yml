image: ubuntu:trusty

pages:
  artifacts:
    paths:
      - public
  environment:
    name: gitlab-static
    url: https://feryardiant.gitlab.io/static-spa
  script:
    - echo 'OK'
  only:
    - master
